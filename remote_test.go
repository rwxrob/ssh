package remote_test

/*
func ExampleSSH() {
	ukey, _ := os.ReadFile(`/tmp/blahpriv`)
	hkey, _ := os.ReadFile(`/tmp/hostpubkey`)
	out, err := remote.SSH(`blah@localhost:22`, ukey, hkey, `ls -ld ~`)
	fmt.Print(string(out))
	fmt.Println(err)
	// Output:
	// ignored
}
*/
